# Fonts (placeholder)

Self-host the house typefaces here (brief §7) and swap the Google Fonts `<link>`
in `src/layouts/Shell.astro` for local `@font-face` rules:

- **Caveat** / **Kalam** — hand-lettered display (titles + the mark)
- **Space Grotesk** — clean sans (body)

Drop the `.woff2` files in this folder. They are served at
`/<base>/fonts/<file>.woff2`.
